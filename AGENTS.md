# Prototype Instructions

Before any work, read `PROJECT-HANDBOOK.md`. It is the canonical project handoff, original 39-site registry (38 currently in scope), priority list, implementation map, and safety boundary. Update it whenever a site's provider, preset, status, verification result, or next step changes.

Before starting research on any new site, first read the **Richy 脚本索引** at the top of `SOURCE-RESEARCH.md` (`docs/Richy*.txt`, 15 scripts). If the target site has a matching Richy script, it already contains the upstream domain, API contract, and playback logic — the fastest possible source of information. Update the index whenever a new script-to-site mapping is discovered.

**研究任何新站点前，还必须在 SleazyFork/GreasyFork 搜索该站点的油猴脚本**（用户 2026-08-18 决定，任务清单固定项）：目标站的去广告/免费看 VIP/保播/下载脚本往往直接暴露上游域名、API 契约与播放逻辑（已验证案例：`kankan`/AVJB 的 `docs/Richy (6).txt`、`hxc`/Fi11 的 SleazyFork 456275「含羞草研究所免费看」脚本）。搜索关键词：站点名（含别名，如含羞草→Fi11）+ 油猴脚本站（sleazyfork.org 优先，含成人向）；发现的脚本要么直接保存到 `docs/Richy*.txt` 并更新索引，要么把关键线索（域名清单/API 路径/播放逻辑）写入 SOURCE-RESEARCH.md。

**站点验收铁律（用户 2026-08-18 定，后续所有站点适用）**：每完成一个站点，必须与参考站（cfnav.me 子站）核对，确保呈现的"牌型"（目录/分类/搜索/详情/播放效果）与参考站一致。**能播放 ≠ 已通过**。目标是把用户可见效果做到与参考站一致（如 VIP 视频全部能播）；**若参考站能播的比我们多，就是我们这边有问题**，必须继续：要么自己找线索（上游探针/逆向），要么反复尝试，要么请用户在参考站 console 执行命令取证（其环境无 `copy()`，用 `console.log` 输出）——取证命令要具体可粘贴，输出贴回后据此修复。做完再核对，核对过才算完成。**实现前教训（hxc 案例）**：动手前先充分核对参考站的列表/排序/分类/搜索/详情/播放接口契约（hxc 首页默认排序就曾用错 `orderType:3` 推荐混排，而参考站是 `orderType:1` 最新，导致首页 24 卡 id 与参考站不一致；参考站真实播放接口是 `/media/playlist/{id}?sort=1` 而非 `/api/video/play`，凭旧文档会误判）——参考站前端 bundle/Network 抓包/console 取证是最快路径。

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

## Product decisions

- This is an authorized, personal, non-commercial learning project inspired by cfnav.me.
- Git commit policy (user decision, 2026-08-14): do NOT push per small change. Only commit/push once a site's channel/adapter is verified stable (headless playable + tests green + no obvious source churn), and group the site's code + docs into one commit. Keep history coarse for maintainability.
- Independence is the priority: do not make the app depend on `cfnav.me/api/*` as its permanent data source.
- Portal fidelity decision (user decision, 2026-08-17): the local root portal must visually and interactively match the live 40-node `cfnav.me` homepage, including the new `sf` / 私房 TV and `98` / 98堂 cards, current ordering/counts, GAME 0 stat, filters, delivery/status badges, leaderboard shell, 佬友优选 shell, account/header controls, hover feedback, and green-passage entry. sf stays pending; 98 now has a verified live provider and may show ONLINE. Leaderboard/优选 must never be populated with fabricated users, titles, scores, or activity; when no independent real data exists, preserve the exact interface with an explicit empty state. Portal card art must use the reference site's official dark thumbnail set (`/assets/previews/thumbs/*.jpg`) copied locally, not mixed light-theme or legacy snapshots.
- Real-time fetching only (user decision, 2026-08-15): ALL providers must fetch their catalog/detail/playback live from upstream at runtime. No snapshot/mirror/one-time-export data files (no `public/*/*.gz` style static data). The `qiying` entry currently violates this with `public/qiying/*.gz` and must be converted to live fetching (its upstream Typecho list pages `/` and `/page/N/` are fetchable, cards are `class="post-card"`).
- User-visible resource behavior (browse, search, open, play/read) matters more than pixel-perfect UI cloning.
- The first video/live screening pass is complete: the genuinely matchable public entries are implemented, while the rest have explicit paid-content, magnet-only, or independence blockers. Do not reopen excluded video sites without new evidence. The unresolved items are `zb`, whose public MP4 objects lack an independent dynamic catalog, and the 41 oxax brand channels in `tv`, which were concluded UNAVAILABLE at deployment on 2026-08-17 (s.oxax.tv 404s datacenter IPs and the `k=` signature binds to the resolving IP; works only locally/dev on residential IPs — see the tv row in PROJECT-HANDBOOK.md; do not reopen without new evidence like a CF-free resolver, IP-unbound signatures, or CORS on oxax.tv). The 39 AdultIPTV theme streams are verified (on 2026-08-16 their playback was fixed after the acceptance pass: cdn.adultiptv.net only serves real TS segments at the root path, so the manifest proxy rewrites segments to `https://cdn.adultiptv.net/{basename}`; asian-girls/blowjob are dead on the CDN; DetailModal now routes suffixless proxy URLs through hls.js). The user asked to skip `asmr` for now; do not request saved ASMR pages again until they resume it.
- `sjs` / 司机社 was decided SKIP on 2026-08-15 (user confirmed after investigation: it is a pure 网盘/磁力 download-resource mirror). Reference site contract (user's logged-in console): `/api/categories` 13 slugs (latest/hot/domestic/western/japanese/vr/ai-video/image-video/anime-2d/anime-3d/doujin/gallery/find-source — slug param has NO effect, whole site is one latest feed), `/api/home` thread stream (id/title/author/date/replies/views), `/api/thread/{id}` returns `{body, images[], links[], media[]}` with links/media ALWAYS empty and hasMedia false — download links (夸克网盘 / Pikpak / 磁力合集 / 解压密码) are buried in body text, threads use 车票 (forum currency) bounties. Upstream is `sjs66.com` (Discuz forum, exposed via body text; attachment image CDN `urlimage.cc/attachments/forum/...` is direct-fetchable 200 real JPEG/GIF) but sjs66.com itself is fully Cloudflare-managed-challenge (Node 403). Same type as `ja` / JavBus (download-only, no online playback) → skipped per the no-magnet/download-flow rule. Do not reopen without new evidence (e.g., online playback on the forum or CF-free entry).
- `book` / 有声读物 was decided SKIP on 2026-08-15 (user confirmed: same type as `mt` — login-walled reference APIs + CF-protected/unknown upstream). Reference site `book.cfnav.me` is a Next.js SPA with 书库/音声 dual modes; ALL `/api/*` return 401 on Node (login wall). 书库 `/api/book18`: GET `?page&sort` (sort: modified/popular/hot/recommended/favorited) → 50/page `{id,title,slug}` (traditional-Chinese titles); POST `{slug}` → `{title, chapters[{nodeId,title}]}`; POST `{nodeId}` → Markdown chapter text (`#NTR #NTL #純愛` tags + author names like fongjia; nodeIds increment numerically 232259~233696+, source looks like a novel site/TG feed, no independent upstream found via search). 音声: `/api/asmr?path=` network-drive directory tree (20-hex-char ids like aliyundrive, isDir/type:audio; 中文音声 311 entries / 13 pages), `/api/asmr-moon?page=` (second library: /中文音声 /日韩音声 /English + README.md), `/api/audio?path=` streams MP3 binary (ID3 tags expose `ASMR.GAY` / `t.me/asmrgay`). Cover CDN `cdn2.createaiasian.com/{hash}.jpg` is direct-fetchable 200 (createaiasian = CreatePorn AI image site, covers only, NOT the content source); ASMR upstream `asmrgay.com` is fully Cloudflare-managed-challenge (Node 403). Conclusion: all data lives behind the reference login-walled APIs + upstream unreachable/unknown → skip like `mt`. Do not reopen without new evidence (e.g., a public upstream for the book catalog or the ASMR files).
- `jm` / 看禁漫天堂 was implemented on 2026-08-15: real upstream is the OFFICIAL NEW SITE `18mh.net` (permanent address; GitLab official repo `18mh-net/18mh-net` created 2026-03-19 confirms identity + mirrors: `32b.azucyfo.com` CF-free, `qkfmoba.cc` entry page, TG `t.me/t_18dm_net`). IMPORTANT divergence: the reference site `jm.cfnav.me` self-describes `source: 18comic.ink` (OLD library, 7-digit ids like 1461564, paths `/album/*`, covers `cdn-msp2.18comic.ink/media/albums/{id}_3x4.jpg?u=`) — but 18comic.ink main site is fully Cloudflare-challenged (Node 403 AND headless Chrome blocked; only the cover CDN is alive), and it is a DIFFERENT data library than 18mh.net (5-digit ids, `/comic/*` paths; reference titles 404 on 18mh.net). Per the independence rule we implemented the official new site. Chains verified: list `/comic/all` (48 cards/page, total ~20476, SSR cards `ul.dx-novel-list`), pagination `/comic/all/page/N`; 12 categories `/comic/all/{slug}` (rb 日本H漫 18882 etc., pagination is `/comic/all/{slug}/{n}` NOT `/page/N` which 404s); rank `/comic/rank` (220 cards), hot/newest/freshest; search `/comic/search/{kw}` first page only; detail `/comic/detail/{id}` (`data-comic-info` JSON type/tags + chapter list `a.detail-page__catalog-item` with `chapter-badge`/`chapter-title` — NOTE class attr uses SINGLE quotes + trailing space and the block exceeds 600 chars so capture limit must be 2000; the 开始阅读 button class `detail-page__read-btn` is NOT matched); chapter `/comic/chapter/{id}/{n}` (54 images/chapter via `data-src`). Images `pic.xmbvxj.cn` are ENCRYPTED bytes (magic `4f e8 97`, auth_key optional) → rewrite to `imgpublic.ycomesc.live{path}` (strip `?auth_key`) same as qiying. Headless verified: 48 cards, category, rank 220, search 姐姐们的调教 12, detail 70 chapters, reader 54 pages all loaded (720×3008), zero JS errors. Fully independent of cfnav.me.
- `xo` / 爱看 was implemented on 2026-08-17: real upstream is 香蕉视频 `h5.xxoo473.org/api` (reference app.js source comment literally says `数据源: https://h5.xxoo473.org` — same-origin proof). Anonymous endpoints: list `/v2/vod/listing-{cateid}-{area}-{year}-{def}-{dur}-{freetype}-{mosaic}-{lang}-{order}-{page}` (16/page, 42815 total), category tree (cateid 16 香蕉原创/5 制服诱惑/6 清纯少女/7 辣妹大奶/8 女同专属/9 素人出演/10 角色扮演/11 成人动漫/12 人妻熟女/13 变态另类), orders (1 好评/2 播放/3 评分), search `/search?wd=&page=`, detail `/vod/show/{id}`. Playback is VIP-locked (`/vod/reqplay` retcode 5) so the adapter mirrors the reference's OWN Richy unlock (app.js comment `Richy-style: preview KEY → full master when reqplay is VIP-locked`): preview m3u8 KEY URI `https://{cdn}/{date}/{id}/{bitrate}kb/hls/key.key` → full master `https://{cdn}/{date}/{id}/index.m3u8` → complete AES-128 HLS. All media CDNs (mymb041.com/k18e7j.com/aqsmimg cover) are CORS `*` direct; browser plays straight through. Headless verified: 16 cards, complete 2035.7s (33:55) master resolved, 1920×1080 readyState=4 advancing, zero JS errors. Fully independent of cfnav.me.
- On 2026-08-14 the user confirmed the sites previously excluded for content reasons have been updated and are back to 待接入 (pending): `mt`, `miss`, `qiying`, `rou`, `tx`, `hqw`, `91`, `mr`, `mm`, `jm`, `book`, `madou`, `best`, `sjs`, `qms`. On 2026-08-15 the user also moved the remaining 11 sites (`one`, `hj`, `hxc`, `dj`, `swag`, `kankan`, `9s`, `dsd`, `xo`, `jav`, `bj`) back to 待接入; the paid/VIP gating rule was removed from all docs.
- `one` / KanOne was decided SKIP on 2026-08-15 (user decision, same type as `mt`: catalog locked behind reference login wall). Reference site is a Next.js SPA; `POST /api/bootstrap` (inside the Linux.do session) returns `{uuid, hotKeywords[59], imgServers[13], cdnList[11]}`, `POST /api/search {uuid,keyword,page,limit}` → items `{id,modelId,thumb(cfnav-private ticket),title,subtitle,isLimitFree,number,publishedAt}` + hasMore, `POST /api/detail {uuid,id}` → `{detail:{title,content(HTML),actor,author,tags,tagList,multiplePic,multiPicThumbnail,videoFile,previewVideo,videoHls(private proxy),videoHlsH265,quality,size,length,views,buys,coin,originalCoin...},streamUrl,mediaKind}` — ALL 401 without login. Independent media chain VERIFIED: image hosts `imgpw807.s7n7ue8.com`/`jmt612.xqjby.com`/`jmtp616.youguancm.com` `/storage/thumb/{id}/{hash}.jpg|webp` → 200 + CORS `*`; MP4 `dlmk0129.scycjz.com` etc. `/one/compress/decry/vd/{date}/{b64}/{time}/{WxH}/aac/h265/mp4/decrypt/{token}.mp4` → 206 + CORS `*` plain ftyp-isom MP4 (no signature/Referer needed); HLS `/encry/vd/.../hls/decrypt/index.m3u8` is encrypted bytes (MP4 suffices). Domain-cluster pattern (same 防封 mirror farm): `dlmk0129`×4 domains, `0325api`×5 (Swoft API at `/home/www/api`), `imgpw807`×2, `1vy79bws04jv/eyf08pws05jv.gdliren123.com`, `kwgewx01dl.mfpt8g.com`; DoH: image/video 23.197.86.x + API 23.62.46.x (Akamai), jmt612 218.12.76.167/121.22.232.169 (China Telecom direct). Upstream Swoft routes unguessable (60+ probes → 500 Route-not-found/403). `jmt612.xqjby.com/` exposes public S3 ListBucketResult (bucket `one-fruit-new`, 1000 keys/page: `/oneVideo/hls/one/{date}/{id}/index.m3u8+ts` 757 keys = 2022 OLD data, `admin/jiami/storage` 222, avatars 14) but bucket files 403, pagination 403 (`marker`/`prefix`/`list-type=2`), so NOT a live catalog. Conclusion: media chain independent, but catalog/search/detail fully behind the reference login wall — same as `mt`/`best`/`qms` catalog-blocker type; user decided SKIP. Re-evaluate only with new evidence: a public upstream web site (not mirror CDNs), exposed 0325api routes, or S3 bucket read access.
- `hj` / 看海角 was implemented on 2026-08-16: real upstream is `www.haijiao.com` (official site; ALL data via anonymous `/api/*` JSON; `isEncrypted:true` means data is TRIPLE Base64: `JSON.parse(atob(atob(atob(text))))` — identical to the reference site's decode.js). Endpoints verified: hot `/api/topic/hot/topics?page=N` (20/page), category tree `/api/topic/nodes_by_ver/v2?ver=` (128 nodes), category posts `/api/topic/node/topics?type=1&nodeId=X&page=N`, search `/api/topic/searchV2?q=&page=&limit=20` (total cap 10000), detail `/api/topic/{id}` (`attachments[]` with images/video categories + `remoteUrl`; `sale.amount` 金币 for paid posts). Images on `pic.hj*.top` are `.txt` files with a CUSTOM base64 alphabet `"ABCD*EFGHIJKLMNOPQRSTUVWX#YZabcdefghijklmnopqrstuvwxyz1234567890"` (`*`=standard `+`, `#`=standard `/`); decode → `data:image/jpeg;base64,...` (verified JPEG). Worker action=img decodes and returns real JPEG. Playback: unpaid posts only get a ~30s preview m3u8 (`{id}_i_preview.m3u8`, 25 segments × 1.25s) on `ts10.hj*.top/hjstore/video/{date}/{hash}/` (CORS `*`, AES-128 with explicit IV); `.key` is a WRAPPED key (fixed 16B) — real key = official wasm `jquery_key(key, r)` where `r` = atob of a `.jpg` file in the same directory as the m3u8 (a FIXED string that leaks upstream's MongoDB credentials — do NOT hardcode it into code; fetch the `.jpg` live every time). The wasm (`/js/jquery.wasm`, 12825B, emscripten single-function module; imports: env `{_abort_js, emscripten_resize_heap}` + wasi `{fd_close, fd_write, fd_seek}`) is embedded base64 in runtime.js with module-level instance cache; verified real key decrypts TS (`47 40 11 10`). `action=media` rewrites the playlist: KEY line URI → `/provider-api/hj?action=key&u={keyUrl}&j={jpgUrl}` proxy, ts lines → absolute direct URLs. FULL-VIDEO (2026-08-16, no 金币 needed): the full video playlist is anonymously fetchable — the preview m3u8 filename uses the ATTACH id (`{attachId}_i_preview.m3u8`) but its ts segment names use a DIFFERENT stem (`{attachId2}Y{hash}_i{n}.ts`); the full playlist is `{attachId2}Y{hash}_i.m3u8` in the same directory (LCP of ts names + `.m3u8`). Verified on 2 topics: 2227731 → `13839670Y6Y1dNJG_i.m3u8` 1664s/1338 segments, 2232393 → `13875705kWCmgZhI_i.m3u8` 992s (= video_time_length), same `enc_*.key`/IV, ts decrypts OK. `hjPlaylist` now tries the guessed full playlist first and falls back to the preview. Detail remarks show full length (视频 · N 分钟). Headless verified: duration 1664s, playback beyond 31s preview limit. UI reuses QiyingPage/QiyingModal (`provider?.id === "hj"`; QiyingModal startPlay now uses dynamic `${provider?.id}`). Headless verified: 20 cards, covers 8/8, detail 14 images, 1080×1920 playback advancing, category + search working, zero JS errors. Fully independent of cfnav.me.
- `qiying` / 栖影 was implemented on 2026-08-14 and converted to fully live fetching on 2026-08-15 (user decision: no snapshot/mirror data files anywhere — see the real-time-only rule above; `public/qiying/*.gz` deleted, `scripts/prepare-qiying-data.mjs` removed). Real upstream is 91吃瓜网 (Typecho, `agency.nsguiiwz.cc` + `being`/`act` mirror lines from its 防失联 page, all 301-redirect to the current host `agency.qxmrdvtu.cc`; `91vip2x` / `91chigua` domains are hijacked/retired). Live endpoints verified: `/` and `/page/N/` (1246 pages × 30 cards; ~15 of each 30 are ad cards with no `<h2 class="post-card-title">` — the parser skips them, home shows 15 real posts/page), `/category/{slug}/` and `/category/{slug}/{n}/` (23 categories from nav, e.g. zxcghl/今日吃瓜 1224 pages, 91th 69 pages; pagination is `/{n}/` NOT `/page/{n}/` which 404s), `/search/{kw}/` (first page only; `/search/{kw}/{n}/` pagination 404s), `/tag/{slug}/`. Detail `/archives/{id}/` and playback are live (DPlayer blocks map 1:1 to `action=play&idx=N`; videos are server-signed `auth_key` m3u8s on `op.vkjyoi.cn`, ts/key on `bgqpnx.cn`, CORS `*`). CRITICAL: raw page images on `pic.*.cn` are ENCRYPTED (non-JPEG magic bytes, e.g. `3e aa 70 8e`); every image (card cover from `loadBannerDirect('...')`, detail gallery, poster) must be rewritten to `https://imgpublic.ycomesc.live{path}` (verified real JPEG, CORS OK) via `qiyingImageUrl()`. Card fields: id/title/author/datePublished/categories/`热搜` badge; search-page cards use absolute `https://arrest.qxmrdvtu.cc/archives/{id}/` URLs so the id regex accepts both forms. No runtime dependency on cfnav.me. The 91吃瓜 videos cannot be played without the main-site signature; do not attempt to reuse cfnav.me `/api/hls`.
- The `ja` / 看 JavBus entry is a metadata-and-magnet directory without online playback. The user explicitly chose to skip it; do not implement or investigate magnet/download flows.
- On 2026-08-14 the user decided to skip `qms` / 秋名山直播: its catalog (131 platforms, ~12000 channels, FLV/MSE) exists but the vast majority of channel streams are hidden behind the reference site's login-walled `/api/stream/{id}` proxy; upstream CDNs (hfjqkc.com / quanyuanhj.com / hesurf.com) return 403 on all probed paths, so an independent real stream URL cannot be derived. Only a few channels expose public Aliyun OSS URLs. Do not reopen qms without new evidence (e.g., a working public stream pattern).
- `madou` / 看麻豆 was implemented on 2026-08-14: real upstream is `madou.club` (麻豆社, WordPress; categories/slugs/post ids match the reference site 1:1). Playback: each detail page has `<iframe src="https://dash.madou.club/share/{shareId}">`; that share page embeds a 100-second JWT (`var token`) + `var m3u8 = '/videos/{shareId}/index.m3u8'`; fetch m3u8 with `?token=`, ts segments and `ts.key` need no token (CORS `*`). Fully independent of cfnav.me, no login needed. week/month pages are empty; likes ranking works. Do not treat `madouai` (麻豆视频 AI) as the same upstream as `madou`.
- `ai` / 麻豆视频 AI (`madouai` provider) was implemented and fixed on 2026-08-16: real upstream is `www.madouai.xyz` (REST API: `/api/v1/videos?page&size&categoryId`, `/api/v1/videos/search?q`, `/api/v1/videos/{id}` → `{title,coverUrl,videoUrl,categoryName,durationSec,viewCount,authorName,...}`; `coverUrl`/`videoUrl` are paths like `jhimage/{date}/{hash}` or `jpg/{date}/{hash}.m3u8` → asset builder maps them to `/api/v1/image/proxy?path=` / `/api/v1/m3u8/proxy?path=`). Playback is AES-128 HLS (`#EXT-X-KEY` URI `/api/v1/m3u8/key`, IV=0), ts segments on `qp*.dcsfik.com` (ACAO `*`, direct). CRITICAL BUG FOUND + FIXED: madouai's m3u8 proxy and key endpoints reflect a DUPLICATED `Access-Control-Allow-Origin` header (e.g. `"http://localhost:5173, http://localhost:5173"`) — browsers reject that response ("Failed to fetch"/ERR_BLOCKED_BY_CLIENT/manifestLoadError) while Node fetch (no CORS check) succeeds, so the local app hung while raw fetches looked fine. Fix: detail's `vod_play_url` is now the same-origin proxy `/provider-api/madouai?action=media&url={direct}` (clean ACAO `*`); `madouAiMedia` proxies ONLY the manifest (`/api/v1/m3u8/proxy`) and key (`/api/v1/m3u8/key`), rewriting `URI="..."` key lines to proxy URLs; ts segments stay direct (dcsfik.com sends single `*`). Host whitelist: `www.madouai.xyz` only. Headless verified after fix: readyState=4, currentTime advancing, 1280 wide, zero JS errors. Fully independent of cfnav.me.
- On 2026-08-14 the user decided to skip `hqw` / 好片: its real upstream `haoqi7.com` (好妻网) is fully cracked technically (anonymous guest signin with AES-256-ECB-encrypted API responses, 14 categories via a SolidStart `/_serverFn` endpoint with Seroval payloads, `.ceb` encrypted covers, signed m3u8 playback), but the site's anti-abuse is severe — it bans the calling IP (`errorCode 1067 此ip已经禁止登陆`) after short bursts of requests, including repeatedly during a single research session across several proxy IPs. A Cloudflare Pages deployment would run on a fixed egress IP and likely get banned quickly. The user judged the complexity/risk not worth one entry and removed the work-in-progress code (git workspace restored to the madou commit `487908d`). Do not reopen hqw without new evidence that the ban is not IP-based (e.g., a stable anonymous session that survives many requests).
- On 2026-08-14 the user decided to skip `mt` / 看蜜桃 for the same reason: the reference site (Next.js SPA) serves every video/cover through cfnav's private short-lived media tickets (`__cfnav_media/m/kan-mt/playlist/{token}`, `/api/media/{token}` -> "media ticket expired", covers via `media.cfnav.com/m/kan-mt/image/*`), the upstream origin is hidden in a server-side env (`SOURCE_ORIGIN`) that the client cannot see, and the reference site itself could not play videos during the research session. No independent upstream exists to build an adapter against. Do not reopen mt without new evidence (e.g., the actual SOURCE_ORIGIN upstream and a public media path).
- `miss` / 看 Miss was implemented on 2026-08-15: real upstream is `missav.media` (public, no login, no VIP/paid signals). Section keys map 1:1 to upstream `/cn/{key}` (new/release/today-hot/weekly-hot/monthly-hot/chinese-subtitle/uncensored-leak/fc2/heyzo/siro). Lists are SSR (12 cards/page, `dm{id}`-prefixed pagination links); search is `/search/{kw}?page=N` (NOT `/search?q=`); genres/actresses/makers index pages are also SSR and used for preset tabs. Media is all public direct links with CORS `*`: covers `fourhoi.mrstcdn.store/{code}/cover-t.jpg` (thumb) / `cover-n.jpg`, preview `preview.mp4`, playback `surrit.mrstcdn.store/{uuid}/playlist.m3u8` (multi-bitrate master; segments are `.jpeg`-disguised TS, no EXT-X-KEY, `#EXT-X-TOKEN` is decorative — no token needed). The uuid is extracted from the detail page via `surrit\.mrstcdn\.store\\?\/([0-9a-f-]{36})`. Headless verification requires clicking `.age-gate button` (localStorage `cf-age`) first or cards are 0. Playback verified: hls.js 640×360 advancing, covers 12/12. Fully independent of cfnav.me. **2026-08-16 BLOCKED by upstream Cloudflare managed challenge**: ALL missav domains (.media/.ai/.ws/.fans/.live/123.com/njavtv.com) now return CF "Just a moment" 403 — full browser fingerprint headers 403, headless Chrome stuck on challenge page, DoH resolves fine (not DNS pollution), missav.com is seized → ThisAV. The reference site `miss.cfnav.me` is a FastAPI backend that fetches missav server-side + `/proxy/*` media proxies (its survival without browser sessions suggests its egress IP passes CF where ours doesn't). Handed to a browser-capable agent (user login state) to verify: user-browser direct access to missav.ai, overseas-IP access, reference `/api/movie/{code}` metadata_links, new CF-free mirrors. Recorded as upstream-CF-blocked; media chain (mrstcdn.store covers/m3u8) itself remains alive.
- `tx` / 看糖心 Vlog was implemented on 2026-08-15: real upstream is `tangxinvlog.pro` (Astro; the reference site's `/api/videos?page=43` error literally returns `Upstream HTTP 404 for https://tangxinvlog.pro/videos/43/`, proving same-origin). 988 works / 42 pages, 46 artists, verified identical to the reference site page-by-page (page 42's 4 items match exactly incl. title spacing). Home 12 latest; `/videos/{n}/` pagination (24/page); `/artists/{name}/` full works on one page (e.g. 饼干姐姐 79); detail has artist/date/duration/tags/description/related 12. NO search on the reference site — the local search box is hidden for tx. Media: `t.5gcdn.xyz` has no CORS and 403s without `Referer: https://tangxinvlog.pro/`, so ALL media (covers, avatars, m3u8, ts, enc.key) goes through the same-origin proxy `/provider-api/tx?action=media&path=videos/{cdnId}/{file}` (path whitelist; Referer/UA added; CORS `*` returned). Playback is AES-128 HLS (`#EXT-X-KEY` URI `enc.key` + explicit IV). CRITICAL: when rewriting the playlist, rewrite BOTH segment lines AND the `URI="enc.key"` key line to absolute proxy URLs — if the key URI stays relative, hls.js resolves it against `/provider-api/tx?...` and requests `/provider-api/enc.key` → 404 → playback hangs forever. Playback verified headless: 1080p (1920×1080) advancing, artist flow (46 → 饼干姐姐 → 24 cards → play) verified. Fully independent of cfnav.me.
- `mr` / 看每日大赛 was implemented on 2026-08-15: real upstream is `mrds.com` (= `www.mrds66.com`, 每日大赛-实时吃瓜爆料平台; Typecho + Mirages, SAME program family as the qiying upstream 91吃瓜网). Same-origin proof via user's logged-in reference export: `/api/meta` 21 category slugs match mrds.com nav 1:1; `/api/posts` totalPages 1691 == mrds home pager 1/1691 (was 1677 on an older check — reference count grows with upstream); first page 30 ids/titles/authors match mrds home cards exactly. Reference `/api/post/{id}` detail contract: `{textBlocks, images[].imageUrl, videos[].playUrl}` — images/playlist are cfnav-private tickets (`media.cfnav.com/m/kan-mr/image/{token}` — Cloudflare challenge 403 on Node; `__cfnav_media/m/kan-mr/playlist/{token}`) so no runtime dependency on them. Reuses the qiying parsing layer verbatim: lists `/`, `/page/N/` (ad cards skipped via missing `h2.post-card-title`), 21 cats `/category/{slug}/` + `/{n}/` pagination (1658 pages), search `/search/{kw}/` first page only, detail `/archives/{id}/` (`data-xkrkllgl` images + DPlayer `data-config` signed m3u8). Media verified: `pic.xustgq.cn` raw images are ENCRYPTED (magic `09 3d e3 b1`) → `qiyingImageUrl()` rewrite to `imgpublic.ycomesc.live{path}` (real JPEG); playback `hls.dscxru.cn/videos5/{hash}/...m3u8?auth_key` + AES-128 key/ts on `ts.syjiaotong.mobi`, ALL CORS `*`, browser direct. Code: new `MR_ORIGIN`/`MR_MIRRORS`, `mrPage`/`mrDetail`/`mrPlay`/`mrList`; `qiyingExtractDetail` gained a `siteName` param (default 91吃瓜网); `qiyingCats` accepts pre-fetched HTML; App.jsx `QiyingPage`/`QiyingModal` parameterized by provider (shared by qiying + mr). Headless verified: list 27 cards + covers 27/27, category 28 cards, pagination 2/1658, search 小千 22, detail gallery, 1280×720 playback advancing, zero JS errors. Fully independent of cfnav.me.
- `rou` / 看肉视频 was implemented on 2026-08-15: real upstream is `rou.video` (Next.js SSR; same-origin proof: the reference site's `/api/video/{id}` returns `siteDomain: "https://rou.video"`, ids match 1:1, search 糖心 39 pages both sides, tag 糖心Vlog 1804/1803, cat tree 198 tags identical). Reference site contract (`/api/home` 9 sections, `/api/video/{id}`, `/api/search`, `/api/tag/{tag}`, `/api/cat`) maps 1:1 to upstream SSR pages: `/home` (latestVideos 16 + dailyHotCNAV/Selfie/91/OnlyFans/JV + hotCNAV/Selfie/91 15 each), `/cat` (gcAV 57 / madouAV 36 / v91 73 / onlyfans 32), `/t/{tag}?order=createdAt&page=N` (26/page), `/search?q={kw}&page=N` (26/page). Detail `/v/{id}`: `ev = {d: base64, k: number}`, decode = `atob(d)` then subtract `k` from each byte then `JSON.parse` → `{videoUrl, thumbVTTUrl}`; signed HLS `v.rn2xx.xyz/hls/{id}/{id}-720/index.jpg?v=6&exp&auth` (~1-day expiry, `.jpg`-disguised m3u8, NO EXT-X-KEY, segments each independently signed on rotating `v.rnNNN.xyz` hosts, 10s each). Covers are imgproxy direct links `v.rn221.xyz/m/{key}/rs:fit:1280:0:0:0/wm:1/{base64('rou/hls/{id}/cover.jpg')}.jpg` — no hotlink protection, `loading="lazy"` in headless means covers only load after scroll. Media has no CORS → playlist/segments/thumbVTT go through `/provider-api/rou?action=media&url={full signed URL}` (host whitelist `^v\.rn\d+\.xyz$` + path `/hls/`; playlist lines rewritten to absolute proxy URLs; segments are `.jpg`-disguised TS, no key rewrite needed). Dev-server note: runtime.js edits need a vite restart (same as tx). Headless verified: 9 sections 137 cards, covers 137/137 after scroll, detail, 720p advancing (1280×720), cat 198 tags, tag list pagination, search 26 cards, zero JS errors. Fully independent of cfnav.me.
- `best` / 看 JavPorn was decided SKIP on 2026-08-15 (user decision, same rationale as `mt`/`qms`). User's userscript `Richy.txt` (BestJavPorn ad-clean v2.5.0) proved upstream `www.bestjavporn.com` (WordPress; blob iframe player + JW Player; real HLS via streamplay). Reference-site contract fully exported from user session: `/api/home` (3 sections ×20 + 9 category keys), `/api/list/{filter}?page=N` (20/page, latest = 15013 pages ≈ 300k items), `/api/search?q=`, `/api/play/{slug}` → `{m3u8_proxy (cfnav-private, unused), m3u8_url (direct)}`. The playback chain IS independently usable (verified): `m3u8_url` = `apiraw2.streamplay.win/data/master.m3u8/{JWT}` → `index-v1-a1.m3u8?data=` → segments on TikTok CDN (`p16/p19-ad-site-sign-sg.tiktokcdn.com/ad-site-i18n-sg/...~tplv-d5opwmad15-ttam-origin.image`, 5s each, ~2-day signed, PNG-shell-wrapped TS needing unwrap — same as reference hls-direct.js `unwrapPngSegment`), all direct-fetchable without cfnav. Cover CDN `pics.pornfhd.com` is CF-free direct (prestige pattern `/mgs/images/prestige/{brand}/{num}/pb_e_{code}.jpg`, e.g. abf-319, dlv-006) but main site `pornfhd.com` returns 522 (dead) so no traversal/pattern coverage for other brands. THE HARD BLOCKER IS THE CATALOG: reference `/api/*` requires Linux.do login (app must not hold user sessions); upstream is fully Cloudflare-managed-challenge except robots.txt (wp-json/search/uploads all 403, Turnstile not passable headless); pornfhd.com main site dead; ~300k live items cannot be snapshot-exported. Re-evaluate only if: upstream opens a CF-free entry, pornfhd.com revives with a public catalog, or the reference API drops its login wall. Do not reopen without new evidence.
- Cloudflare Pages support is deployed at `video-4nn.pages.dev`. The 39 AdultIPTV theme streams in `tv` are now verified playing locally (2026-08-16 fix: root-path segment rewrite + suffixless proxy URL HLS routing); the 41 oxax brand channels were concluded UNAVAILABLE at deployment on 2026-08-17 (see the tv row in PROJECT-HANDBOOK.md; do not reopen without new evidence); 98 dmn12 is verified at the Pages edge; `asmr` is paused by user decision.
- 2026-08-17 final local Chrome acceptance (real browser, no reference-site dependency, no code changes): 16 of 17 in-scope sites fully pass — list → detail → play/read advancing with no manifestLoadError / Failed to fetch / CORS failures: `ai` (2 items sampled), `hj`, `91`, `mr`, `qiying`, `tx`, `rou`, `pmv`, `lg`, `fj`, `ep`, `tna`, `movie`, `madou`, `jm` (reader 23/23 pages); `tv` theme streams pass (MyCamTV MILF readyState=4), the oxax channels still wait for deployment-level verification. Full table in PROJECT-HANDBOOK.md (2026-08-17 entry). The site is now deployed to Cloudflare Pages (`video-4nn.pages.dev`); deployment headless spot-check (same day) found `miss` UNBLOCKED and fully playable from the CF edge (list 12 + covers 12/12 + 640×360 playback advancing — the CF edge egress passes the upstream managed challenge; the local 0-card 502 conclusion is void), `tv` theme-stream manifest proxying works (oxax brand channels remain unsolvable at the edge: s.oxax.tv 404s CF datacenter IPs and the stream `k=` signature is bound to the resolver's IP, so CF-signed URLs fail from residential IPs — commit `8dcc692` now 302-redirects oxax manifests to the direct signed stream, which works locally/dev and keeps edge traffic direct), `ep` was 502 at the edge and is FIXED (commit `e577966`: the direct-fetch branch compared provider.id against "ep" but the catalog key is "eporner", so it never matched; now list/detail/health-check all fetch www.eporner.com/api/v2 straight from the browser, ACAO `*`, verified 24 cards + embed iframe at the edge), qiying/mr/hj/jm lists+detail render fine. A real-browser full acceptance pass by a browser-capable agent is pending; fix list will follow its results. Decoy gate now redirects every locked path (incl. `/site/*`) to `/` via history.replaceState with `index.html` title/description also decoyed to nginx (commit `0007324`), unlock restores real title and shows a brief welcome toast. Next up after acceptance: probing the 2 new reference sites (`sf` 私房 TV, `98` 98堂) for future adapters.
- The `game` / 看板娘入口 is explicitly out of scope by user decision and must not be restored to navigation or implementation work.
- Do not create, upload, save, or deploy this project to OpenAI Sites unless the user explicitly asks for that exact deployment in a future message. The intended target is local use or infrastructure controlled by the user; read-only capability checks do not authorize a deployment.
- If the user later asks for deployment, the preferred target is Cloudflare Pages for the static app and lightweight API resolution. Do not deploy yet. Do not route sustained third-party video segment traffic through Pages Functions/Workers by default; keep direct media URLs direct and place any unavoidable HLS relay on separately approved infrastructure after checking provider and hosting terms.
- Main-directory fidelity is now a product requirement: preserve the reference site's exact entry names where known, prefer real per-site/upstream cover imagery over generic placeholders, reproduce its denser card information and hover feedback, and avoid presenting the navigation as a sparse experimental dashboard. Do not claim pixel-perfect fidelity until the saved reference homepage and hover state have been compared directly.
- Fidelity rule: never use a same-category substitute provider or keyword preset to claim a reference sub-site is implemented. A route is complete only after its actual reference catalog, fields, detail flow, and playback/resource chain are verified. Unverified routes must show a clear pending state rather than unrelated fallback content.
- Put every source behind a replaceable adapter with health reporting and keep private sessions, cookies, keys, and user data out of the project.
- 2026-08-17 98堂正式适配结论：`dmn12.vip` Discuz 分类/搜索/详情 HTML 与参考条目同源，`play.php` JSONP 实时生成 `tyjs.ypxjft.cn` AES-128 HLS；Pages 边缘已通过年龄页流程并验证列表/搜索/详情/manifest/key/TS，`98` 已注册到 `PROVIDERS`/`ROUTE_CONFIGS`。本地 Node 可能因源站 CF challenge 失败，Pages 是可用出口；`sf` 仍保持 pending。
- 2026-08-18 **ph（Pornhub 实验来源）定案：前端走 Vercel relay**。CF 边缘被 phncdn 按 IP 自适应限速（持续请求 0.1–0.3MB/s、挂 20s）→ 弃用；Vercel 出口持续 11–46MB/s 满速。App.jsx 的 `PH_RELAY_BASES` 默认 `https://ph-vercel-probe.vercel.app/api`（单账号，用户决策；`localStorage.phRelayBases` 逗号分隔可加多账号，`phRelayFetch` 自动轮换+记忆 `phRelayIndex`）。**这套「无 CORS + 签名绑定出口 IP」的媒体中继模式已成型，未来站点遇到同类障碍直接复用**：relay 负责抓详情/列表、代理媒体（封面防盗链 + 清单分片绝对化 + 补 ACAO `*`），前端 hls.js 播 relay 返回的绝对 URL。ph 仍不写入 ROUTE_CONFIGS、不进门户、不入 health check。Vercel Hobby 100GB 带宽/月是硬上限；多账号绕额度违反 ToS，用户已知情。relay 代码在 `Temp\opencode\ph-vercel-probe`，真实播放测试页 `Temp\opencode\ph-relay-playtest.html`。
- 2026-08-18 **用户新定最高原则（后续所有站点适用）**：第一优先级 = **复刻参考站（cfnav.me 子站）能看到的效果**；上游必须**尽量与参考站的上游一致**；用户可在参考站 console 配合执行命令取证（注意其环境无 `copy()`，用 `console.log`）。
- 2026-08-18 **`9s` / 看九色 已接入**（2026-08-18）：真实上游 = `jiuse.tv`（别名 `91porny.com`）。同源铁证：参考站 `/api/home` 首页第一项 id `b2cbcc3adbbc4ad5bb52` 与 jiuse 首页第一项逐字一致；14 分类 slug 契约一致。参考站是 Next.js 登录墙（Node 全 401），`/api/video/video/{id}` 返回的 m3u8 是 `__cfnav_media` 私有票据不可用 → 全部直抓 jiuse。实现要点：首页 = `/`+`/vod`+`/videos` 三路合并 17+12+12=41 卡（与参考首页同款裁剪）；14 分类 `/video/{slug}` 分页 `/video/category/{slug}/{n}`；搜索 `/video/search?q=`（仅首页）；三 kind 详情（video `/video/view/{id}` `data-src` m3u8 三线路 `?server=lineN`、vod `/vod/view/{id}`、videos `/videos/view/{id}/{slug}/` `video#video-play` MP4 直链 cdn.cdnkdjs.com）；**媒体全 CORS `*`、不查 Referer、未加密 → 浏览器直连零代理**（区别于 ph 的 relay 路线）；封面 http→https 归一直连。坑：`ROUTE_CONFIGS` 里 `"9s":` 必须加引号（`9s` 裸键非法，`91:`/`98:` 数字字面量合法）；`hd` 分类在 jiuse 全广告页 → 本端如实空态；React 受控 input 需原生 value setter + 点搜索按钮。headless 验收：41 卡/0 破图/15 tabs/搜索 24 卡/三 kind 播放推进/零错误。勿在无新证据时重开 `hd`（上游即空）或尝试作者页（404 不存在）。
- 2026-08-18 **`jav` / 看 JAV 已接入**（2026-08-18）：真实上游 = `javhd.com`。同源铁证：参考站 `/api/home` sourceLink 指向 javhd 详情页 URL、标题逐字一致；参考 `/api/play` 4 码率签名直链与 `javhd.com/zh/player_api?videoId={pid}&is_trailer=0` 匿名返回一致。侦查链：static2.javhd.com（未被墙；static.javhd.com 是 SNI 级阻断 CDN77 全 IP 000）拿到 `player/jplayer/jplayer.min.js` → 详情页 SSR `<player-component content-path="/zh/player_api?videoId={pid}&is_trailer=1">` → 匿名签发接口。**列表/搜索 = 页面 URL + 请求头 `X-Requested-With: XMLHttpRequest`** → `{status, template:<thumb-component>}`：列表 `/zh/japanese-porn-videos` + 分页 `/zh/japanese-porn-videos/justadded/all/{n}`（36 卡/页，507 页）；搜索 `/zh/search?q=&page=`（60 卡/页）；卡片属性 `video-id`（=playerId）/`link-content`（详情 URL）/`url-thumb`/`video-preview`/`title`/`has-label`（premium/premiumFree）/`views`/`likes`。**详情**：SSR 页（必须纯浏览器头，XHR 头会 500）解析 content-path 取 playerId + og:title/og:image；**播放**：`/zh/player_api?videoId={pid}&is_trailer=0` → sources[4]（1080p/720p/480p/240p `_sh/_hq/_med/_low.mp4`）c3.cdnjhd.com 签名直链（token+expiry 时效、206、CORS `*`、H.264+AAC、moov 前置标准 MP4），浏览器原生 video 直连；免费与付费视频均匿名 FULL 完整源（抽查 12/12，付费墙只是 GUEST UI 层）。实现：catalog.js `jav` + runtime.js `javFetch/javParseCards/javPageCount/javList/javDetail/javPlay` + App.jsx SitePage 接线 + ROUTE_CONFIGS `jav` + parseStreams 优先 `item.streams`（码率标签）。坑：列表分页是路径式 `/justadded/all/{n}` 不是 `?page=`（query 的 page 被忽略）；详情页禁用 XHR 头；javhd 偶发 ECONNRESET 需重试 ×3 + 1.5s 退避；**headless 验收必须 GPU 模式（`--use-angle=swiftshader`；`--disable-gpu` 下 Chrome 无法起播该 MP4 停在 readyState=0，真机浏览器无此问题，非源故障）**。headless 验收：36 卡/0 破图/详情+4 码率/1920×1082 播放推进/搜索 yuki 60 卡/分页 2/零错误。零 cfnav 依赖。
